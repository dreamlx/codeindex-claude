{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "command": "${CLAUDE_PLUGIN_ROOT}/hooks/check-codeindex.sh",
        "description": "Verify the codeindex CLI is available on PATH; print actionable install hint if not."
      }
    ]
  }
}
